import 'package:flutter/material.dart';

class AllSectionList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('All Section List'),
      ),
      body: Center(
        child: Text('Welcome to All Section List!'),
      ),
    );
  }
}
